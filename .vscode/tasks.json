{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "test_blocks",
            "type": "shell",
            "command": "./tests/test_blocks.py",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "clean",
            "type": "shell",
            "command": "rm -rf graphs/*.gv*",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}